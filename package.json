{
  "name": "ads-pipeline",
  "private": true,
  "version": "1.0.0",
  "description": "Lambda functions and Aurora schema for the ad compliance validator ingestion pipeline (SPEC.md section 3.1)",
  "scripts": {
    "test": "node --test",
    "migrate": "node db/migrate.js"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "@aws-sdk/client-secrets-manager": "^3.700.0",
    "@aws-sdk/s3-request-presigner": "^3.700.0",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
